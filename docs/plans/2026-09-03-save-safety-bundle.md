# 저장 안전성 소형 수정 묶음 계획서 (2026-09-03)

> 2026-09-02 Mac 프런트 결함 탐색(워크플로우 렌즈 1개, 재현 11건)에서 나온 데이터 유실급 결함을 묶어 수정한다. 사용자 결정(2026-09-03): 단계 5A D1 을 이어가기 전에 이 묶음을 먼저 처리한다. 발견 원문과 재현 테스트는 `~/wiki-handoffs/2026-09-02_slidecaptain-mac-session/` 에 있고, 로드맵 이월표에 등재되어 있다.
> 브랜치 `feature/save-safety` (main 에서 분기). 완료 후 main 에 fast-forward 머지한다.
> 리뷰 게이트: 계획서 적대 리뷰 1회 → 태스크별 TDD 구현(태스크마다 커밋) → 커밋별 독립 리뷰와 브랜치 최종 리뷰를 한 리뷰어가 수행(소형 묶음 한정 축약. Critical 묶음과 같은 방식).

## 범위와 원인 묶음

| 태스크 | 포함 발견 | 공통 원인 |
|---|---|---|
| A. 저장 상태 기계 | FC-01 저장 중 되돌리기 유실(major), FC-04 겹친 PUT 역순 착지(major, 이월표 기등재), FC-03 저장 대기 정체(minor), FC-10 불안정 콜백으로 PUT 연사(minor, 잠재) | `useDeckEditor` 가 "저장할 것이 있는가" 를 참조 비교(`deck === savedDeck.current`)로만 판단하고, 저장을 직렬화하지 않으며, 저장이 착지한 뒤 그사이 생긴 변경을 다시 저장하지 않는다 |
| B. 낡은 미리보기 편집 차단 | FC-05 낡은 계획으로 편집 시 다른 불릿 덮어쓰기(major), FC-02 실측 실패가 저장 성공에 지워짐(major) | 미리보기가 편집 대상 인덱스와 초기 텍스트를 덱이 아니라 서버 렌더 계획에서 가져오는데, 덱이 바뀐 뒤 새 계획이 오기까지 낡은 계획이 편집 가능한 채로 남는다. 실측 오류와 저장 오류가 한 상태를 공유한다 |
| C. 플러시 미대기 경로 | FC-08 목록 복귀 시 플러시 실패 무표시(major), FC-14 탭 전환이 플러시 실패를 무시(major, 기등재), FC-11 스냅샷 복구 진입이 플러시를 기다리지 않음(minor), FC-16 생성 중 목록 버튼 미잠금(minor, 기등재) | `ProjectView` 의 화면 이탈 경로 네 곳 중 내보내기만 플러시 결과를 검사한다 |

이 묶음에 넣지 않는 것: FC-07 보고 정보 저장 응답 전 탭 전환, FC-12 자료 탭 입력 무저장 이탈(자료 탭의 저장 모델 문제라 별도), FC-09 템플릿 전환 안내, FC-06 클라이언트 오류 상세, FC-13 리듀서 밖 apply, FC-15 beforeunload(단계 5A A 의 원래 항목이며 A 의 버전 비교가 선행 조건이라 다음 묶음), FC-17 업로드 중 탭 잠금.

## 태스크 A: 저장을 직렬화하고 착지 뒤 잔여 변경을 다시 저장한다

**재현 (2026-09-02, 인계 폴더 `useDeckEditor.critical.test.tsx` F1, F3, F4, F10)**

- F1: E1 저장 PUT 진행 중 Ctrl+Z 로 S 로 돌아오면, 자동 저장 효과가 `deck === savedDeck.current`(S) 로 조기 반환하고, PUT 착지 뒤 `savedDeck = E1` 이 되지만 S 를 다시 저장하는 코드가 없다. 서버는 E1, 화면은 S, 표시는 '저장 중' 영구 정체. 탭 전환이나 내보내기의 플러시만이 S 를 올린다.
- F4: E1 PUT 진행 중 E2 편집 → 두 PUT 동시 진행 → E2 응답이 먼저 오면 '저장됨', 뒤늦은 E1 응답이 `savedDeck` 을 E1 로 되돌리지만 재저장은 없다. 서버 최종 = E1, 화면 = E2, 표시 '저장됨'.
- F3: 저장 타이머 전 Ctrl+Z 로 저장본과 같아지면 '저장 대기' 표시가 남는다.
- F10: `onDeckChange` 가 렌더마다 새 함수면 언마운트 플러시 효과(deps `[saveNow]`)가 저장 중 렌더마다 정리 함수를 실행해 PUT 을 연사한다 (실측 200ms 에 36회). 현재 호출자(ProjectView)는 안정된 `setDeck` 을 넘겨 발동하지 않는다.

**변경 (`frontend/src/state/useDeckEditor.ts`)**

- 저장을 **한 줄로 직렬화**한다: `saveChain` ref 에 진행 중 저장 프라미스를 두고, `saveNow(target)` 은 그 뒤에 이어 붙인다. 겹친 PUT 이 없어지므로 역순 착지(F4)가 원천 차단된다.
- **착지 뒤 잔여 변경 재저장**: PUT 성공 뒤 `deckRef.current !== target` 이면(그사이 편집이나 되돌리기가 있었으면) `deckRef.current` 를 즉시 다시 저장 예약한다(체인에 붙인다). F1 의 S 가 이 경로로 올라간다. 되돌리기로 저장본과 같아진 경우는 "저장할 것 없음" 으로 끝난다.
- **저장할 것 없음의 판정**: `doPut(target)` 은 `target === savedDeck.current` 면 PUT 없이 true 를 돌려준다. 자동 저장 효과의 조기 반환 분기는 진행 중 저장이 없으면 `setSaveState("저장됨")` 으로 표시를 되돌린다(F3). 진행 중 저장이 있으면 그 착지 처리가 표시를 정한다.
- **콜백 안정화**: `onDeckChange` 를 ref 에 담아 `saveNow` 가 ref 를 호출하게 하고, 언마운트 플러시 효과의 deps 를 빈 배열로 둔다(F10). `flushSave` 는 진행 중 체인을 먼저 기다린 뒤 잔여 변경을 저장한다.
- 스냅샷 규칙(첫 저장, `snapshotNext`)과 `replace`, `undo`, `redo`, `apply` 의 의미는 바꾸지 않는다.

**테스트 (실패부터, `frontend/src/state/useDeckEditor.save.test.tsx`)**: F1 → 착지 뒤 두 번째 PUT 이 자동으로 S 를 보내고 최종 상태가 '저장됨' 이다. F4 → PUT 은 순차로 나가고(둘째는 첫째 착지 뒤 시작) 마지막 PUT 본문이 E2 다. F3 → 되돌리기로 저장본과 같아지면 '저장됨' 이다. F10 → 불안정한 콜백을 넘겨도 200ms 동안 PUT 은 1회다. 저장 실패 뒤 다음 편집이 저장을 시도한다(체인이 실패 상태에 머물지 않는다). `flushSave` 는 진행 중 저장이 끝난 뒤 잔여 편집까지 올린다. 추가로 **무작위 시나리오 불변조건 테스트**(고정 시드로 편집, 되돌리기, 다시 실행, 지연 착지, 실패 1회를 섞은 시퀀스 30개): 모든 프라미스가 정착하면 저장 상태는 '저장됨' 이고 서버가 보유한 덱(마지막 성공 PUT 본문)이 화면 덱과 같다 (적대 리뷰 범위 판정 반영). 기존 `EditorScreen.test.tsx` 의 저장 관련 단언(첫 저장 스냅샷, 플러시)은 그대로 통과해야 한다.

**실행 중 발견 (2026-09-03, 무작위 시나리오 시드 13, 22)**: 처음 쓴 불변조건 "마지막 PUT 본문 = 화면 덱" 은 틀렸다. 실패한 PUT 은 서버 상태를 바꾸지 않으므로 비교 대상은 마지막 성공 PUT 이다 (부하에 따라 순서가 달라져 전체 실행에서만 간헐 실패했고, 사건 기록을 붙여 재구성했다). 같은 기록에서 훅의 표시 결함 하나가 나왔다: 저장 중 되돌리기로 화면이 저장본과 같아진 뒤 그 PUT 이 실패하면 덱이 바뀌지 않아 자동 저장 효과가 다시 돌지 않고 표시가 '저장 실패' 에 머문다. 실패 처리에서 `deckRef.current === savedDeck.current` 면 '저장됨' 으로 두되 오류 문구는 남긴다(서버 문제 자체는 알린다). 결정적 회귀 테스트 1건을 추가했고, 실패 1회가 플러시의 PUT 에 걸릴 수 있으므로 시나리오는 실패 시 플러시를 한 번 더 부른다.

## 태스크 B: 낡은 미리보기에서는 편집을 열지 않고, 실측 오류를 저장 오류와 분리한다

**재현 (인계 폴더 `EditorScreen.staleplan.test.tsx` F5, `useDeckEditor.critical.test.tsx` F2)**

- F5: 덱 [A, B, C] 에서 속성 패널로 A 를 삭제하면 저장본은 [B, C] 인데 미리보기는 새 계획이 올 때까지(디바운스 300ms + 왕복) [A, B, C] 를 그린다. 그 창에서 B 를 클릭해 "B2" 로 고치면 `applyTextEdit` 이 낡은 인덱스 1 로 C 를 덮는다. 결과 [B, B2]. 사용자가 고친 B 는 그대로고 C 가 사라진다.
- F2: 실측이 실패하면 `error` 에 문구가 뜨지만 1.2초 뒤 저장이 성공하면서 `setError("")` 가 지운다. 미리보기는 편집 전 계획에 머물고 재실측 경로가 없어 다음 편집까지 화면과 저장본이 어긋난 채 유지된다.

**변경**

- `useDeckEditor`: 계획이 어느 덱을 기준으로 계산됐는지 `planDeck` ref 로 기억하고 `planStale = plan !== null && planDeck !== deck` 을 돌려준다. 실측 오류는 `measureError` 로 분리하고 저장 성공은 `saveError` 만 지운다. 실측 실패 시 `planStale` 은 참으로 남는다. 재실측을 요청하는 `remeasure()` 는 별도 호출 경로를 만들지 않고 `measureTick` 상태를 올려 **기존 디바운스 실측 효과를 재실행**한다 (효과의 `cancelled` 가드가 늦게 도착한 응답을 걸러 준다. 적대 리뷰 F6).
- `EditorScreen`: `Preview` 에 `editable={!editor.planStale}` 을 넘기고, 실측 오류가 있으면 오류 문구와 "다시 그리기" 버튼(`remeasure`)을 함께 보여준다. 저장 오류는 종전대로 표시한다.
- `Preview`: `editable` 이 거짓이면 `startEdit` 를 무시하고(프레임 선택은 유지) 캔버스에 `stale` 클래스를 붙인다(흐림 표시. `styles.css` 한 줄). 기본값은 참이라 기존 호출자와 테스트는 그대로다(F7). 낡은 계획으로는 어떤 문단도 편집 대상이 되지 않으므로 잘못된 인덱스 저장이 원천 차단된다. 이미 열려 있는 인라인 편집은 다른 곳을 클릭할 때 브라우저가 textarea 의 blur 를 먼저 일으켜 변경 전 상태로 먼저 확정되므로 안전하다. 이 가정은 코드 주석에 남긴다(F4).

**테스트 (실패부터, `frontend/src/screens/EditorScreen.stale.test.tsx`)**: F5 → A 삭제 뒤 새 계획이 오기 전에는 B 를 클릭해도 "내용 수정" 입력이 열리지 않고 두 번째 PUT 이 없다. 새 계획이 도착하면 편집이 열리고 [B, B2] 가 아니라 [B2, C] 가 저장된다. F2 → 실측 실패 뒤 저장이 성공해도 실측 오류 문구가 남고 "다시 그리기" 로 실측이 재시도되며 성공하면 문구가 사라진다. 기존 `Preview.test.tsx` 의 인라인 편집 단언은 `editable` 기본값 참으로 그대로 통과해야 한다.

## 태스크 C: 화면 이탈 경로 전부가 플러시 결과를 검사한다

**재현 (인계 폴더 `ProjectView.back.test.tsx` F8, 코드 근거 F14, F11)**

- F8: 편집 직후 '목록으로' 는 `onBack` 을 곧바로 호출해 편집기를 내리고, 언마운트 플러시의 PUT 실패는 이미 내려간 컴포넌트에 대한 `setState` 라 어디에도 표시되지 않는다. 목록 화면에는 오류를 받을 자리가 없다.
- F14: `switchTab` 은 `await flushEditor.current()` 의 반환값을 버리고 탭을 바꾼다. 실패하면 다음 탭(구조안 draft, 자료 meta)이 낡은 덱으로 초기화되고, 구조안 승인의 PUT 이 미저장 편집을 확정적으로 덮는다.
- F11: '스냅샷 복구' 는 플러시를 기다리지 않아 언마운트 플러시 PUT 이 복원 POST 뒤에 착지하면 복원본이 덮인다.

**변경 (`frontend/src/screens/ProjectView.tsx`)**

- 공통 함수 `leaveEditor(action: string): Promise<boolean>` 이 `flushEditor.current` 를 기다리고, 실패하면 배너("마지막 편집을 저장하지 못해 {action}를 중단했습니다. 저장 상태를 확인한 뒤 다시 시도해 주세요.")를 띄우고 false 를 돌려준다. `switchTab`("이동"), '목록으로'("이동"), '스냅샷 복구'("이동"), 내보내기("내보내기")가 전부 이 함수를 거친다. 내보내기의 기존 문구와 그 테스트(`ProjectView.test.tsx` "내보내기를 중단")는 그대로 유지된다 (적대 리뷰 F1).
- '목록으로' 와 '스냅샷 복구' 버튼은 생성 중(`generating`)과 이탈 처리 중(`leaving`) 잠근다(F16, 적대 리뷰 F2. 내보내기의 `exporting` 잠금과 같은 관례).
- 실행 중 보정 (2026-09-03): 배너 문구의 조사는 인자에 포함한다 ("이동을", "내보내기를"). 위 문안대로 `{action}를` 로 조립하면 "이동를" 이 된다. 실패 경로 테스트는 "PUT 정확히 1회" 대신 "편집기가 그대로 떠 있다" 로 단언한다: 디바운스 타이머의 재시도 PUT 이 정당하게 한 번 더 나갈 수 있어 횟수 단언은 시간 의존이다.

**테스트 (실패부터, `frontend/src/screens/ProjectView.flush.test.tsx`)**: F8 → PUT 이 실패하면 목록으로 나가지 않고 배너가 뜬다. PUT 이 성공하면 PUT 뒤에 목록 화면이 뜬다. F14 → 실패 시 탭이 바뀌지 않고 배너가 뜬다. F11 → 복구 화면은 플러시 착지 뒤에 열린다. F16 → 생성 중 '목록으로' 가 비활성이다.

## 실행 순서

태스크 A → 태스크 B → 태스크 C → 문서 정정 → 독립 리뷰 → 수정 반영 → main 머지 → push.

문서 정정: 로드맵 이월표의 해당 행(FC-01, 05, 08 신규 3행과 FC-04, 14, 16 기등재 3행)을 처리 완료로, 진행 상태에 이 묶음 항목 추가. 적대 리뷰가 찾은 기존 결함 1건을 이월표에 신규 등재: `replace()` 가 켠 `snapshotNext` 가 디바운스 전 되돌리기로 소비되지 않은 채 남아 무관한 다음 저장에 스냅샷이 찍힌다(F3. 이 묶음은 스냅샷 규칙을 바꾸지 않으므로 범위 밖). 설계서 7.2(저장 사고 대비)에 "저장은 직렬화하고 착지 뒤 잔여 변경을 재저장한다", 6절(편집 화면)에 "미리보기는 최신 덱 기준 계획일 때만 편집을 연다" 를 날짜와 사유를 달아 추가.

## 이 계획이 틀렸을 가능성

- 저장 직렬화로 체인이 길어지면 빠른 연속 편집에서 마지막 저장이 늦어진다. 디바운스(1.2초)가 이미 편집을 묶어 주므로 실사용에서 체인 길이는 1~2 다. 관찰되면 "체인 실행 시점에 `deckRef.current` 를 보내 중간 저장을 합치는" 최적화를 다음 묶음에서 한다.
- 낡은 계획일 때 편집을 막으면 300ms 남짓의 창에서 클릭이 무시된다. 흐림 표시가 그 이유를 알려 주지만, 파일럿에서 "클릭이 안 먹는다" 는 관찰이 나오면 클릭을 대기열에 두는 방식으로 바꾼다.
- 편집기의 저장 실패로 화면 이탈이 막히면 서버가 죽은 상태에서 사용자가 갇힌다. 배너에 "저장 상태를 확인" 안내를 두고, 스냅샷 복구는 서버가 살아 있을 때만 의미가 있으므로 같은 규칙을 적용해도 실해가 없다. 서버 사망 시의 탈출구(강제 이탈)는 5A A 의 재시도 수단 항목에서 다룬다.

## 독립 리뷰 반영 (2026-09-03)

리뷰어 1명이 커밋별 리뷰와 브랜치 최종 리뷰를 함께 수행했다(소형 묶음 한정 축약). 판정 **승인**, 커밋 5건 전부 승인.

| 항목 | 결과 | 반영 |
|---|---|---|
| TDD 실증 | 각 태스크 직전 커밋에 신규 테스트만 이식해 실행: A 8건, B 3건, C 4건 실패 확인 (형식적 TDD 아님) | 해당 없음 |
| 경쟁 조건 추적 6가지 (재귀 잔여 저장과 체인, 플러시와 타이머, 실패 시 '저장됨' 분기, planStale 참조 비교, leaving 과 언마운트, 기존 테스트 충돌) | 전부 안전 판정. 근거: 재귀가 체인 프라미스 안에 포함되어 순서가 유지되고, `target === savedDeck` 가드가 중복 PUT 을 흡수하며, 리듀서와 slotOps 가 항상 새 객체를 만들어 참조 동일성이 내용 동일성을 뜻한다 | 해당 없음 |
| 발견 7 (minor) | `leaving` 잠금이 목록으로와 스냅샷 복구에만 걸려, 플러시 진행 중 탭 전환이나 내보내기를 누르면 두 leaveEditor 가 겹친다. 데이터 유실은 없으나 사라진 컴포넌트에 setTab 이 떨어진다 | e8c3e68: 탭 버튼 3개와 내보내기 버튼도 `leaving` 으로 잠그고 테스트 단언 추가 |
| 발견 8 | Preview 의 "열려 있는 편집창은 blur 가 먼저" 가정을 코드베이스에서 검증: 편집 화면이 열린 채 덱을 바꾸는 외부 경로는 GeneratePanel 의 명시적 반영 클릭뿐이라 성립 | 해당 없음 |
| 검증 실행 | tsc 통과, vitest 120건 통과, 시간 의존 3파일 3회 반복 43건 전부 통과 | 반영 후 재실행 120건 통과 |
| 문서 정합 | 이월표 처리 완료 7행과 잔존 표기, snapshotNext 등재, 설계서 6절과 7.2 전부 코드와 일치 | 해당 없음 |

